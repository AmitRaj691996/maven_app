pipeline{
    agent any
    stages{
        stage('Audit Stage'){
            steps{
                auditStage()
            }
        }
    }
}

void auditStage(){
    sh '''
    git --version 
    java --version
    mvn --version
    '''
}
